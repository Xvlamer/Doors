local G = game
local RS = G:GetService("RunService")
local P = G:GetService("Players")

local lp = P.LocalPlayer

if getgenv().Xvlamer_VClipRunning then
    getgenv().XvlamerStop_VClip = true
    task.wait(0.1)
end

getgenv().XvlamerStop_VClip = false
getgenv().Xvlamer_VClipRunning = true

local char = lp.Character or lp.CharacterAdded:Wait()
local hum = char:WaitForChild("Humanoid")
local hrp = char:WaitForChild("HumanoidRootPart")

local oldSpeed = hum.WalkSpeed
local oldJumpPower = hum.JumpPower
local oldJumpHeight = hum.JumpHeight
local oldPlatformStand = hum.PlatformStand
local oldAutoRotate = hum.AutoRotate

local conns = {}
local vec0 = Vector3.zero
local physState = Enum.HumanoidStateType.Physics
local runningState = Enum.HumanoidStateType.Running
local lockedY = hrp.Position.Y

local cleanedUp = false
local function cleanup()
    if cleanedUp then return end
    cleanedUp = true
    
    for i = 1, #conns do
        pcall(function() conns[i]:Disconnect() end)
    end
    table.clear(conns)
    
    pcall(function()
        if char and char.Parent then
            local descendants = char:GetDescendants()
            for i = 1, #descendants do
                local part = descendants[i]
                if part:IsA("BasePart") then
                    part.CanCollide = true
                    part.Massless = false
                end
            end
        end
        if hrp and hrp.Parent then 
            hrp.AssemblyLinearVelocity = vec0 
            hrp.AssemblyAngularVelocity = vec0
        end
        if hum and hum.Parent then
            hum.PlatformStand = false
            hum.AutoRotate = true
            hum.WalkSpeed = (oldSpeed > 0 and oldSpeed or 16)
            hum.JumpPower = oldJumpPower
            hum.JumpHeight = oldJumpHeight
            hum:ChangeState(runningState)
        end
    end)
    
    getgenv().Xvlamer_VClipRunning = false
    getgenv().XvlamerStop_VClip = nil
end

pcall(function()
    lockedY = hrp.Position.Y
    hum.PlatformStand = true
    hum.AutoRotate = false
    hum.WalkSpeed = 0
    hum.JumpPower = 0
    hum.JumpHeight = 0
    hum:ChangeState(physState)
    
    local descendants = char:GetDescendants()
    for i = 1, #descendants do
        local part = descendants[i]
        if part:IsA("BasePart") then
            part.CanCollide = false
            part.Massless = true
        end
    end
end)

table.insert(conns, RS.Stepped:Connect(function()
    if getgenv().XvlamerStop_VClip then
        cleanup()
        return
    end
    if not char or not char.Parent or not hrp or not hrp.Parent or not hum or not hum.Parent or hum.Health <= 0 then
        cleanup()
        return
    end

    hum.WalkSpeed = 0
    hum.JumpPower = 0
    hum.JumpHeight = 0
    hum.PlatformStand = true
    
    pcall(function() hum:ChangeState(physState) end)
    
    local descendants = char:GetDescendants()
    for i = 1, #descendants do
        local part = descendants[i]
        if part:IsA("BasePart") then
            part.CanCollide = false
            part.Massless = true
        end
    end
end))

table.insert(conns, RS.RenderStepped:Connect(function(dt)
    if getgenv().XvlamerStop_VClip then
        cleanup()
        return
    end
    if not hrp or not hrp.Parent then return end
    
    local cam = workspace.CurrentCamera
    local lookDir = cam and cam.CFrame.LookVector or hrp.CFrame.LookVector
    local flatForward = Vector3.new(lookDir.X, 0, lookDir.Z)
    if flatForward.Magnitude > 0 then
        flatForward = flatForward.Unit
    else
        flatForward = Vector3.new(0, 0, 0)
    end
    
    -- Set velocity to match movement so the server/other players see it and anti-cheat doesn't rubberband
    local targetVel = flatForward * 1.75
    hrp.AssemblyLinearVelocity = Vector3.new(targetVel.X, 0, targetVel.Z)
    hrp.AssemblyAngularVelocity = vec0
    
    local currentPos = hrp.Position
    local nextPos = currentPos + (targetVel * dt)
    hrp.CFrame = CFrame.new(nextPos.X, lockedY, nextPos.Z) * (hrp.CFrame - hrp.CFrame.Position)
end))

table.insert(conns, RS.Heartbeat:Connect(function()
    if getgenv().XvlamerStop_VClip then
        cleanup()
    end
end))

table.insert(conns, hum.Died:Connect(function()
    cleanup()
end))
